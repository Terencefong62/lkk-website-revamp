{
  "name": "LKK Website Revamp",
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 3000",
      "description": "Serves the static site from the repository root at http://localhost:3000. Absolute asset paths (/assets, /modules, /zh-cn) resolve against the repo root."
    }
  ],
  "ports": [
    {
      "name": "Static site",
      "port": 3000
    }
  ]
}
